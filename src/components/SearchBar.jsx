import SearchInput from './SearchInput';
import { TopBarWrapper } from '../styles/Search';

const SearchBar = () => {
    return (
        <TopBarWrapper>
            <SearchInput />
        </TopBarWrapper>
    );
};

export default SearchBar;
